# Datasets

Shared sample data lives here only when it supports more than one lesson or project.

- [`sample-kafka-event.json`](sample-kafka-event.json) is an example nested event payload for Kafka and JSON-processing practice. No current lesson reads it automatically; instructors may use it as a classroom sample.
